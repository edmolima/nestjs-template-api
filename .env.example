# Local Postgres settings for development
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres

# App
PORT=3000
NODE_ENV=development
 
# Whether TypeORM should synchronize the database schema on startup (true/false).
# Use `false` in production and manage schema with migrations.
SYNCHRONIZE=true